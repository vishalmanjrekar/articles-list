Step 1 : Install the node modules by following command 
         ==> npm install

Step 2 : For run project in locally following command
         ==> npm start

Step 3 : For run unit test type following command in termial
         ==> npm run test


